NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-or-anon-key

# Local dev: http://localhost:4000 (run `cd backend && npm run dev`).
# Once the frontend itself is deployed, set this to the live backend
# instead: https://fitech-02.onrender.com — as an environment variable on
# whatever platform hosts the frontend, not in this file.
NEXT_PUBLIC_API_URL=http://localhost:4000
